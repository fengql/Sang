package com.sang.common;

public class CoreConstants {
	
	/**
	 * Sang project configure prefix and now is "SangConfig",which means
	 * "SangConfig*" will match it.The constant is used to determine
	 * configure file in config package by ConfigLisenter to resolve.
	 */
	public static final String configPrefix = "SangConfig";
}
